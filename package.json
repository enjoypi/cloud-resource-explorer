{
  "name": "cloud-resource-explorer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "start": "node dist/frameworks/cli.js",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@11.0.0-dev.1005",
  "devDependencies": {
    "fast-check": "^4.5.3",
    "ts-node": "^10.9.2",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
